import {
  LitElement,
  html,
  css,
  styleMap,
} from "https://cdn.jsdelivr.net/gh/lit/dist@2.4.1/all/lit-all.min.js";

// Grabs all of the things I need from the lit CDN and puts them in one place, makes imports nicer
export { LitElement, html, css, styleMap };
